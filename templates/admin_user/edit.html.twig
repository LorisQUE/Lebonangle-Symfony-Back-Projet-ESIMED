{% extends 'base.html.twig' %}

{% block title %}Administrateur - Édition{% endblock %}

{% block body %}
    <br/>

    <h1>Édition de l'administrateur {{ admin_user.username }}</h1>

    <hr>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('Valider') }}</button>
        <a class="btn btn-secondary" href="{{ path('admin_user_index') }}">Retour</a>
    {{ form_end(form) }}


{% endblock %}
